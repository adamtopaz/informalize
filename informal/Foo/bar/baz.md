# Foo.bar.baz

`Foo.bar.baz` refines the `Foo.bar` fixture and is used where tests need a
distinct, deeper location id.

Keeping this as its own file makes it easier to expand with details over time.
