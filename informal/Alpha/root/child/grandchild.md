# Alpha.root.child.grandchild

This fixture exercises multi-level dotted ids that map to nested directories.

It is intentionally separate from other Alpha terms so each informal location
can evolve independently.
